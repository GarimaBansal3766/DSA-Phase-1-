///Steps to performs recursion there are 4 things to keep in mind 
In main function
1. initialisation 
2. start of function call
In user defined fucntion 
1. base case - the condition to stop 
2. work - what to do the main work to do i will do it one time and rest work will be done by function  the work will be same  but number will pe different passed as parameter.
3. recursive case - how are we iterating 
//****************************************************************************************************************************************//
ques :- print table of 2 using recursion()
#include <iostream>
using namespace std;
void Table(int i , int n)
{
  if(i==n+1)
  {
    return;
  }
  cout<<"2 * "<<i <<"="<< 2*i<<endl;;
  Table(i+1,n);
}
int main() 
{
    int i = 1;
    Table(i,10);
}
//****************************************************************************************************************************************//
//sum of digits (gfg)
class Solution {
    void sumSolve(int n , int &sum)
    {
        if(n==0)
        {
            return;
        }
        int a = n%10;
        sum = sum + a;
        sumSolve(n/10,sum);
    }
  public:
    int sumOfDigits(int n) {
        int sum = 0;
        sumSolve(n,sum);
        return sum;
    }
};
//****************************************************************************************************************************************//
Ques - Factorial (gfg)
// Function to calculate factorial of a number.
int Solve(int n)
{
    //base case
    if(n==1)
    {
        return 1 ;
    }
    //Recursive case 
    return n*Solve(n-1);
}
int factorial(int n) {
    // code here
    
    return Solve(n);
}
//****************************************************************************************************************************************//
//Binary search to find target using recursion 
#include <iostream>
using namespace std;
int binary(int arr [], int target, int l, int h )
{
  if(l>h)
  {
    return -l ;
  }
  int mid = (l+h)/2;
  if(arr[mid]==target)
  {
    return mid;
  }
  else if(target>arr[mid])
  {
  return binary(arr,target,mid+1,h);
  }
  else{
     return binary(arr,target,l,mid-1);
  }
  
}
int main() 
{
  int arr[6] = {1,2,3,4,5,6};
  int l = 0;
  int h = 5;
 int index = binary(arr,4,l,h);
 cout<<index;
}
//****************************************************************************************************************************************//
